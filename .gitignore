\# Engineering Task.md
.claude/settings.local.json

# Python
.venv/
__pycache__/
*.py[cod]
*$py.class

# pytest / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
# coverage_report/ is committed per REQUIREMENTS A15 (HTML report folder).
# Carve it in defensively in case a future *.html / coverage* rule is added.
!coverage_report/

# uv keeps the lockfile committed but caches go ignored
.uv/

# Secrets — only .env.example is committed
.env
.env.local
